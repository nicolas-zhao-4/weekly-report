### 本周进展
- 阅读了2篇论文 `Training-Free Stylized Abstraction` 和 `Abstraction in Style`,前者使用vlm的prompt得到风格正确的图,接着使用ODE将该图退回噪声,在还原的中期加入原始图作为结构引导得到风格化结果; 后者将abstraction明确的解耦成结构抽象和风格抽象,先对原图矢量化提取前景,再训练2个lora,一个从输入的前景的草图到风格化结构图,一个从结构图到最终风格化结果
- 一个模糊的想法: 如果说目标是生成抽象矢量/矢量动画,终归是生成矢量,如果是直接生成矢量表示,那么vllm是最自然的选择(可变长的有结构的一串参数),但是已经有人试过了,直接微调qwen2.5 vl; 我想这个方向再下去,就是上参数,优化数据;  目前读的论文大家许多都想方设法整各种活,利用某些模型的先验知识,  那我可不可以这样: 
	- (可选)对输入栅格图抽象矢量化
	- 已有首帧的矢量图  :  vllm指挥(发出下一个关键帧的prompt)->文+图生图模型->下一帧->sds回传梯度,或者把结果作为优化目标->下一个关键帧矢量    这样还有一个好处,就是比较适配lottie的语法,lottie里头,一段动画是不允许控制点数量和拓扑结构改变的,要变化必须在视觉上打个幌子,插入新的动画模拟这个效果;  在生成下一帧的时候或许还能检测一下拓扑结构有没有改变......
### 问题
- 对研究方向有点迷茫
### 下周计划
- 读读论文再想想.?